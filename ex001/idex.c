#include <stdio.h>

int main(void)
{
    int x, y, s;

    printf("valor do x: ");
    scanf("%d", &x);

    printf("valor do y: ");
    scanf("%d", &y);

    s = x + y;

    printf("a soma dos numeros e: %d", s);
}